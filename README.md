# VisionGuard 🎥🔍

> Микросервис для анализа видео с производственных камер и выявления нарушений

## 📖 О проекте

VisionGuard - это сервис для автоматического анализа видеопотоков с производственных камер. Система обнаруживает нарушения техники безопасности (отсутствие каски, пересечение границ зоны и т.п.) с использованием компьютерного зрения.

Данная реализация - это тестовое задание, демонстрирующее минимальный рабочий прототип с фокусом на инженерной основе.

## 🛠️ Технологический стек

- **Python 3.9+** - основной язык разработки
- **FastAPI** - веб-фреймворк для REST API
- **PostgreSQL** - база данных для хранения результатов
- **OpenCV** - библиотека для анализа видео
- **Docker & docker-compose** - контейнеризация
- **Prometheus** - метрики и мониторинг

## 🚀 Быстрый старт

### Предварительные требования

- Docker и docker-compose
- Python 3.9+ (для локальной разработки)

### Запуск через Docker

```bash
# Клонировать репозиторий
git clone https://github.com/shuldeshoff/visionguard.git
cd visionguard

# Запустить сервисы
docker-compose up --build
```

Сервис будет доступен по адресу: `http://localhost:8000`

### Локальная разработка

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# venv\Scripts\activate  # для Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить PostgreSQL
docker-compose up -d db

# Применить миграции
python scripts/init_db.py

# Запустить приложение
uvicorn src.main:app --reload
```

## 📡 API Endpoints

### POST /analyze
Анализирует видеофайл на наличие движения в кадре.

**Пример запроса через curl:**
```bash
curl -X POST "http://localhost:8000/analyze" \
  -H "accept: application/json" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@video.mp4"
```

**Пример ответа:**
```json
{
  "id": 1,
  "filename": "video.mp4",
  "motion_detected": true,
  "frames_analyzed": 150,
  "processing_time": 2.34,
  "status": "completed",
  "created_at": "2025-11-12T10:30:00"
}
```

### GET /metrics
Возвращает метрики в формате Prometheus.

**Пример запроса:**
```bash
curl http://localhost:8000/metrics
```

**Пример ответа:**
```
# HELP videos_processed_total Total number of videos processed
# TYPE videos_processed_total counter
videos_processed_total 42

# HELP videos_processing_time_seconds Average video processing time
# TYPE videos_processing_time_seconds gauge
videos_processing_time_seconds 2.34

# HELP videos_processing_errors_total Total number of processing errors
# TYPE videos_processing_errors_total counter
videos_processing_errors_total 3
```

### GET /health
Проверка состояния сервиса.

```bash
curl http://localhost:8000/health
```

## 📊 Prometheus метрики

Сервис экспортирует следующие метрики:

- `videos_processed_total` - общее количество обработанных видео
- `videos_processing_time_seconds` - среднее время обработки
- `videos_processing_errors_total` - количество ошибок при обработке
- `videos_motion_detected_total` - количество видео с обнаруженным движением

## 🗄️ База данных

PostgreSQL таблица `video_analyses`:

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL PRIMARY KEY | Уникальный идентификатор |
| filename | VARCHAR(255) | Название видеофайла |
| motion_detected | BOOLEAN | Обнаружено ли движение |
| frames_analyzed | INTEGER | Количество проанализированных кадров |
| processing_time | FLOAT | Время обработки (секунды) |
| status | VARCHAR(50) | Статус обработки |
| error_message | TEXT | Сообщение об ошибке (если есть) |
| created_at | TIMESTAMP | Время создания записи |

## 📁 Структура проекта

```
visionguard/
├── src/
│   ├── main.py              # Точка входа FastAPI
│   ├── api/
│   │   ├── endpoints.py     # API маршруты
│   │   └── models.py        # Pydantic модели
│   ├── services/
│   │   ├── video_analyzer.py  # Логика анализа видео
│   │   └── metrics.py         # Prometheus метрики
│   ├── db/
│   │   ├── database.py      # Подключение к БД
│   │   └── models.py        # SQLAlchemy модели
│   └── config.py            # Конфигурация
├── scripts/
│   └── init_db.py           # Инициализация БД
├── tests/
│   ├── test_api.py
│   └── test_video_analyzer.py
├── docker/
│   ├── Dockerfile
│   └── init.sql
├── docs/
│   ├── PROJECT_PLAN.md      # План разработки
│   └── ANALYSIS.md          # Анализ задания
├── docker-compose.yml
├── requirements.txt
├── .env.example
├── .gitignore
├── LICENSE
└── README.md
```

## 🧪 Тестирование

```bash
# Запустить все тесты
pytest

# С покрытием
pytest --cov=src tests/

# Только unit-тесты
pytest tests/unit/

# Только интеграционные тесты
pytest tests/integration/
```

## 📝 Примеры использования

### Через Postman

1. Создать новый POST запрос на `http://localhost:8000/analyze`
2. В разделе "Body" выбрать "form-data"
3. Добавить ключ "file" с типом "File"
4. Выбрать видеофайл
5. Отправить запрос

### Через Python

```python
import requests

url = "http://localhost:8000/analyze"
files = {"file": open("video.mp4", "rb")}
response = requests.post(url, files=files)
print(response.json())
```

## 📋 Статус разработки

- [x] Инициализация репозитория и документации
- [ ] Настройка FastAPI и базовой структуры
- [ ] Реализация эндпоинта POST /analyze
- [ ] Интеграция OpenCV для анализа видео
- [ ] Настройка PostgreSQL и моделей БД
- [ ] Реализация эндпоинта GET /metrics
- [ ] Докеризация приложения
- [ ] Написание тестов
- [ ] Документация API (Swagger)
- [ ] Финальное тестирование

## 🔧 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Database
POSTGRES_USER=visionguard
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=visionguard_db
DATABASE_URL=postgresql://visionguard:secure_password@db:5432/visionguard_db

# Application
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=False

# Video processing
MAX_VIDEO_SIZE_MB=100
FRAME_SAMPLE_RATE=5
```

## 🐛 Известные ограничения

- Максимальный размер видеофайла: 100MB
- Поддерживаемые форматы: MP4, AVI, MOV
- Анализ выполняется синхронно (для больших файлов рекомендуется использовать очереди)

## 🚀 Планы развития

См. [`docs/PROJECT_PLAN.md`](docs/PROJECT_PLAN.md)

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 📞 Контакты

- GitHub: [@shuldeshoff](https://github.com/shuldeshoff)
- Репозиторий: [visionguard](https://github.com/shuldeshoff/visionguard)

---

**Версия**: 0.1.0 (MVP)  
**Дата создания**: Ноябрь 2025

