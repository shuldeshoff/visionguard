# Руководство по контрибуции

Спасибо за интерес к проекту VisionGuard! Мы рады любой помощи.

## Как внести вклад

### 1. Форкните репозиторий

Создайте форк проекта на GitHub.

### 2. Клонируйте репозиторий

```bash
git clone https://github.com/ваш-username/visionguard.git
cd visionguard
```

### 3. Создайте ветку для изменений

```bash
git checkout -b feature/ваша-фича
# или
git checkout -b bugfix/название-бага
```

### 4. Настройте окружение для разработки

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

### 5. Внесите изменения

- Следуйте стилю кода проекта
- Добавьте тесты для новой функциональности
- Обновите документацию при необходимости

### 6. Проверьте код

```bash
# Форматирование кода
black src/
isort src/

# Линтинг
flake8 src/

# Типизация
mypy src/

# Тесты
pytest tests/ -v --cov=src
```

### 7. Закоммитьте изменения

Используйте понятные сообщения коммитов:

```bash
git commit -m "feat: добавлена новая функция X"
git commit -m "fix: исправлена ошибка в Y"
git commit -m "docs: обновлена документация Z"
```

**Префиксы коммитов:**
- `feat:` - новая функциональность
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление/изменение тестов
- `chore:` - обновление зависимостей, конфигурации

### 8. Отправьте изменения

```bash
git push origin feature/ваша-фича
```

### 9. Создайте Pull Request

Создайте PR на GitHub с описанием ваших изменений.

## Стандарты кода

### Python стиль

- Следуем **PEP 8**
- Используем **Black** для форматирования (line length: 100)
- Используем **type hints** везде, где возможно
- Пишем **docstrings** для всех публичных функций и классов

### Пример docstring

```python
def analyze_video(video_path: str, threshold: float = 0.02) -> dict:
    """
    Анализирует видео на наличие движения.
    
    Args:
        video_path: Путь к видеофайлу
        threshold: Порог детекции движения (0.0-1.0)
        
    Returns:
        Словарь с результатами анализа
        
    Raises:
        VideoNotFoundError: Если видеофайл не найден
        InvalidVideoError: Если видео повреждено
    """
    pass
```

### Тестирование

- Покрытие тестами должно быть > 80%
- Пишите unit-тесты для всех новых функций
- Добавляйте integration-тесты для API endpoints

```python
def test_analyze_motion_detects_movement():
    """Тест проверяет, что анализатор детектирует движение"""
    analyzer = VideoAnalyzer()
    result = analyzer.analyze('tests/fixtures/motion_video.mp4')
    assert result['motion_detected'] is True
```

## Структура проекта

```
visionguard/
├── src/              # Исходный код
│   ├── api/          # API endpoints
│   ├── services/     # Бизнес-логика
│   ├── db/           # База данных
│   └── utils/        # Утилиты
├── tests/            # Тесты
├── docker/           # Docker конфигурация
└── docs/             # Документация
```

## Типы вклада

### 🐛 Исправление ошибок

1. Найдите issue с меткой `bug`
2. Создайте ветку `bugfix/название`
3. Исправьте ошибку
4. Добавьте тест, воспроизводящий баг
5. Создайте PR

### ✨ Новая функциональность

1. Создайте issue с описанием функциональности
2. Дождитесь обсуждения/одобрения
3. Создайте ветку `feature/название`
4. Реализуйте функциональность
5. Добавьте тесты
6. Обновите документацию
7. Создайте PR

### 📝 Документация

1. Исправления опечаток - сразу PR
2. Большие изменения - сначала issue
3. Добавьте примеры использования

### 🧪 Тесты

1. Увеличение покрытия всегда приветствуется
2. Добавление edge cases
3. Performance тесты

## Процесс ревью

1. **Automated checks** - CI должен пройти успешно
2. **Code review** - минимум 1 approver
3. **Testing** - все тесты должны проходить
4. **Documentation** - документация обновлена

## Вопросы?

- Создайте issue с меткой `question`
- Напишите в Discussions на GitHub

## Лицензия

Внося вклад, вы соглашаетесь, что ваш код будет лицензирован под MIT License.

---

Спасибо за ваш вклад! 🙏

