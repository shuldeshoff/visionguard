# Анализ Тестового Задания: VisionGuard

## 🎯 Задача

Разработать микросервис для анализа видео с производственных камер и выявления нарушений техники безопасности.

**Тип задания**: Python Developer (инфраструктура + видео)

**Контекст**: Система автоматического мониторинга производственной безопасности с использованием компьютерного зрения.

---

## 📋 Требования задания

### Функциональные требования

#### 1. REST API
- **POST /analyze** - прием видеофайла, анализ движения, возврат результата
- **GET /metrics** - метрики в формате Prometheus

#### 2. Технологии (обязательные)
- Python 3.9+
- FastAPI
- PostgreSQL
- OpenCV
- Docker / docker-compose

#### 3. Функциональность анализа
- Проверка наличия движения в кадре
- Сохранение результатов в БД
- Метрики: количество обработанных видео, среднее время, количество ошибок

### Что должно быть в репозитории
- ✅ Код приложения
- ✅ Dockerfile и docker-compose.yml
- ✅ SQL-скрипт для создания таблицы
- ✅ README.md с инструкциями
- ✅ Примеры запросов (curl/Postman)

---

## 🔍 Детальный анализ

### Что оценивается в задании

#### 1. Инженерные навыки (40%)
- Архитектура приложения
- Структура кода
- Паттерны проектирования
- Масштабируемость решения

#### 2. Технические навыки (30%)
- Знание FastAPI
- Работа с PostgreSQL
- Использование OpenCV
- Docker и контейнеризация

#### 3. Качество кода (20%)
- Читаемость
- Тестирование
- Обработка ошибок
- Логирование

#### 4. Документация (10%)
- README с инструкциями
- Комментарии в коде
- Примеры использования

---

## 🏗️ Архитектурное решение

### Выбранная архитектура: Layered Architecture

```
┌─────────────────────────────────────────┐
│          API Layer (FastAPI)            │
│  - Endpoints                            │
│  - Request/Response models              │
│  - Validation                           │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│         Service Layer                   │
│  - Business logic                       │
│  - VideoAnalyzer (OpenCV)               │
│  - Metrics (Prometheus)                 │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│       Data Access Layer                 │
│  - Repository pattern                   │
│  - SQLAlchemy ORM                       │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│          PostgreSQL                     │
└─────────────────────────────────────────┘
```

### Преимущества выбранной архитектуры

1. **Разделение ответственности** - каждый слой отвечает за свою задачу
2. **Тестируемость** - легко тестировать каждый слой отдельно
3. **Масштабируемость** - легко добавлять новые фичи
4. **Поддерживаемость** - понятная структура для других разработчиков

---

## 💡 Технические решения

### 1. Детекция движения в видео

#### Выбранный подход: Frame Differencing

**Почему:**
- ✅ Простота реализации
- ✅ Быстрая обработка
- ✅ Низкое потребление ресурсов
- ✅ Достаточная точность для MVP

**Алгоритм:**
```python
1. Читать видео покадрово
2. Преобразовать кадры в grayscale
3. Применить Gaussian blur для шумоподавления
4. Вычислить разницу между соседними кадрами (cv2.absdiff)
5. Применить threshold для бинаризации
6. Подсчитать процент изменённых пикселей
7. Если изменений > порога → движение обнаружено
```

**Альтернативы (для будущего):**
- Background Subtraction (MOG2) - более точный, но медленнее
- Optical Flow - для определения направления движения
- ML модели (YOLO) - для детекции конкретных объектов

### 2. FastAPI vs Flask

**Выбрали FastAPI, потому что:**
- ✅ Автоматическая генерация OpenAPI документации
- ✅ Pydantic для валидации данных
- ✅ Встроенная поддержка async/await
- ✅ Высокая производительность
- ✅ Type hints из коробки

### 3. SQLAlchemy ORM vs Raw SQL

**Выбрали ORM:**
- ✅ Безопасность (защита от SQL injection)
- ✅ Читаемость кода
- ✅ Простота миграций
- ✅ Типизация

### 4. Prometheus vs другие метрики

**Prometheus:**
- ✅ Стандарт в индустрии
- ✅ Pull-модель (сервер сам собирает метрики)
- ✅ Богатая экосистема (Grafana)
- ✅ Поддержка в Kubernetes

---

## 📊 Схема базы данных

### Таблица: video_analyses

```sql
CREATE TABLE video_analyses (
    id SERIAL PRIMARY KEY,
    filename VARCHAR(255) NOT NULL,
    motion_detected BOOLEAN NOT NULL,
    frames_analyzed INTEGER NOT NULL,
    processing_time FLOAT NOT NULL,
    status VARCHAR(50) NOT NULL,  -- 'completed', 'failed', 'processing'
    error_message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы для оптимизации
CREATE INDEX idx_status ON video_analyses(status);
CREATE INDEX idx_created_at ON video_analyses(created_at DESC);
CREATE INDEX idx_motion_detected ON video_analyses(motion_detected);
```

**Обоснование полей:**
- `id` - уникальный идентификатор
- `filename` - для отслеживания обработанных файлов
- `motion_detected` - основной результат анализа
- `frames_analyzed` - для метрик производительности
- `processing_time` - для оптимизации и мониторинга
- `status` - для отслеживания состояния обработки
- `error_message` - для debugging
- `created_at` - для аналитики и отчётов

---

## 🔒 Безопасность

### Валидация входных данных

```python
class VideoValidator:
    MAX_FILE_SIZE = 100 * 1024 * 1024  # 100 MB
    ALLOWED_FORMATS = ['video/mp4', 'video/avi', 'video/quicktime']
    
    @staticmethod
    def validate(file: UploadFile) -> None:
        # 1. Проверка размера
        if file.size > VideoValidator.MAX_FILE_SIZE:
            raise VideoTooLargeError()
        
        # 2. Проверка MIME типа
        if file.content_type not in VideoValidator.ALLOWED_FORMATS:
            raise UnsupportedFormatError()
        
        # 3. Проверка расширения
        ext = Path(file.filename).suffix.lower()
        if ext not in ['.mp4', '.avi', '.mov']:
            raise InvalidExtensionError()
```

### Защита от атак

1. **File Upload attacks** - валидация типа и размера файлов
2. **SQL Injection** - использование ORM
3. **DDoS** - можно добавить rate limiting
4. **Path Traversal** - безопасное сохранение файлов

---

## 📈 Метрики и мониторинг

### Prometheus метрики

```python
# Counter - всегда растёт
videos_processed_total = Counter(
    'videos_processed_total',
    'Total number of videos processed'
)

# Gauge - может увеличиваться и уменьшаться
videos_processing_time_seconds = Gauge(
    'videos_processing_time_seconds',
    'Average video processing time in seconds'
)

# Counter - для ошибок
videos_processing_errors_total = Counter(
    'videos_processing_errors_total',
    'Total number of processing errors',
    ['error_type']  # Метка для типа ошибки
)

# Histogram - для распределения времени обработки
processing_duration_seconds = Histogram(
    'processing_duration_seconds',
    'Video processing duration distribution'
)
```

### Что отслеживаем

1. **Производительность**
   - Среднее время обработки
   - P95, P99 латентность
   - Throughput (видео/час)

2. **Надёжность**
   - Количество ошибок
   - Success rate
   - Uptime

3. **Бизнес-метрики**
   - Количество обработанных видео
   - Процент видео с движением
   - Популярные форматы

---

## 🧪 Стратегия тестирования

### Уровни тестирования

#### 1. Unit тесты (60% покрытия)
```python
# tests/unit/test_video_analyzer.py
def test_detect_motion_returns_true_for_video_with_movement():
    analyzer = VideoAnalyzer()
    result = analyzer.detect_motion('tests/fixtures/with_motion.mp4')
    assert result is True

def test_detect_motion_returns_false_for_static_video():
    analyzer = VideoAnalyzer()
    result = analyzer.detect_motion('tests/fixtures/static.mp4')
    assert result is False
```

#### 2. Integration тесты (30% покрытия)
```python
# tests/integration/test_api.py
def test_analyze_endpoint_with_valid_video(test_client, sample_video):
    with open(sample_video, 'rb') as f:
        response = test_client.post(
            '/analyze',
            files={'file': ('test.mp4', f, 'video/mp4')}
        )
    
    assert response.status_code == 200
    data = response.json()
    assert 'motion_detected' in data
    assert data['status'] == 'completed'
```

#### 3. E2E тесты (10% покрытия)
```python
# tests/e2e/test_full_flow.py
def test_full_video_analysis_flow():
    # 1. Загрузить видео
    # 2. Проверить запись в БД
    # 3. Проверить метрики
    # 4. Проверить логи
```

### Цели по покрытию
- ✅ Общее покрытие: >80%
- ✅ Критический код: 100%
- ✅ API endpoints: 100%
- ✅ Утилиты: >70%

---

## ⚡ Производительность

### Оптимизации

#### 1. Sampling кадров
```python
# Не обрабатывать каждый кадр, а каждый N-ый
FRAME_SAMPLE_RATE = 5  # Каждый 5-й кадр

for i, frame in enumerate(frames):
    if i % FRAME_SAMPLE_RATE == 0:
        analyze(frame)
```

#### 2. Уменьшение разрешения
```python
# Resize кадра для ускорения обработки
PROCESSING_WIDTH = 640
PROCESSING_HEIGHT = 480

frame_resized = cv2.resize(frame, (PROCESSING_WIDTH, PROCESSING_HEIGHT))
```

#### 3. Ранний выход
```python
# Если уже обнаружено движение в X% кадров, можно остановиться
MOTION_THRESHOLD = 0.1

if motion_frames / total_frames > MOTION_THRESHOLD:
    return True  # Дальше не обрабатываем
```

### Ожидаемая производительность

| Размер видео | Длительность | Время обработки |
|--------------|--------------|-----------------|
| 10 MB        | 30 сек       | ~2-3 сек        |
| 50 MB        | 2 мин        | ~8-12 сек       |
| 100 MB       | 5 мин        | ~20-30 сек      |

---

## 🚀 План развития (Post-MVP)

### Фаза 1: Асинхронная обработка
```
FastAPI → RabbitMQ → Celery Worker → PostgreSQL
```

**Преимущества:**
- Неблокирующий API
- Параллельная обработка
- Retry механизм
- Масштабируемость

### Фаза 2: ML модели для детекции нарушений

**Возможности:**
1. Детекция отсутствия каски (helmet detection)
2. Детекция защитного жилета (vest detection)
3. Детекция пересечения опасных зон
4. Детекция опасного поведения

**Стек:**
- YOLO v8 или v9
- TensorFlow/PyTorch
- ONNX для оптимизации

### Фаза 3: Stream processing

**Real-time обработка видеопотоков:**
- RTSP streams
- WebRTC
- Kafka для stream processing
- WebSocket для real-time уведомлений

### Фаза 4: Kubernetes deployment

```yaml
# helm chart структура
visionguard/
├── Chart.yaml
├── values.yaml
└── templates/
    ├── deployment.yaml
    ├── service.yaml
    ├── ingress.yaml
    ├── configmap.yaml
    └── hpa.yaml  # Horizontal Pod Autoscaler
```

---

## 💰 Оценка ресурсов

### Требования к серверу (для MVP)

**CPU:** 2-4 ядра  
**RAM:** 4-8 GB  
**Диск:** 50 GB (для временных видео)  
**Network:** 100 Mbps

### Docker образы

**Размеры:**
- Python base: ~150 MB
- + OpenCV: ~300 MB
- + Зависимости: ~50 MB
- **Итого:** ~500 MB

**Оптимизация:**
- Multi-stage build
- Alpine-based образы
- Удаление кешей pip

---

## 🎓 Ключевые технические решения

### 1. Обработка больших видеофайлов

**Проблема:** Видео 100MB может долго загружаться  
**Решение:** Streaming upload + chunked processing

```python
@app.post("/analyze")
async def analyze_video(file: UploadFile):
    # Сохраняем файл по частям
    async with aiofiles.open(temp_path, 'wb') as f:
        while chunk := await file.read(1024 * 1024):  # 1MB chunks
            await f.write(chunk)
```

### 2. Временное хранилище

**Проблема:** Куда сохранять загруженные видео?  
**Решение:** 
- Временная директория `/tmp/uploads/`
- Автоудаление после обработки
- Cleanup по cron для забытых файлов

```python
import tempfile
import os

temp_dir = tempfile.mkdtemp(prefix='visionguard_')
try:
    # Process video
    pass
finally:
    shutil.rmtree(temp_dir)  # Cleanup
```

### 3. Database connection pooling

**Проблема:** Создание нового соединения для каждого запроса - дорого  
**Решение:** Connection pool через SQLAlchemy

```python
engine = create_engine(
    DATABASE_URL,
    pool_size=10,          # Размер пула
    max_overflow=20,       # Дополнительные соединения
    pool_timeout=30,       # Timeout получения соединения
    pool_recycle=3600      # Переиспользование через час
)
```

---

## 📝 Чеклист готовности

### Must Have (обязательно)
- [ ] POST /analyze работает
- [ ] GET /metrics возвращает Prometheus метрики
- [ ] PostgreSQL сохраняет результаты
- [ ] OpenCV детектирует движение
- [ ] Docker compose запускает все сервисы
- [ ] README с инструкциями
- [ ] SQL скрипт инициализации
- [ ] Примеры curl запросов

### Should Have (желательно)
- [ ] Тесты с покрытием >80%
- [ ] Обработка ошибок
- [ ] Логирование
- [ ] Swagger документация
- [ ] Health check endpoint
- [ ] Валидация входных данных
- [ ] CI/CD pipeline

### Nice to Have (бонусы)
- [ ] Helm chart для Kubernetes
- [ ] Async обработка (Celery)
- [ ] Rate limiting
- [ ] Более сложный алгоритм детекции
- [ ] Grafana dashboard
- [ ] Docker multi-stage build
- [ ] Type checking (mypy)

---

## 🎯 Критерии успешной сдачи

### Что точно проверят

1. **Работоспособность** (30%)
   - Сервис запускается через docker-compose
   - API отвечает на запросы
   - Видео анализируется корректно

2. **Архитектура** (25%)
   - Чистая структура кода
   - Разделение на слои
   - Паттерны проектирования

3. **Качество кода** (20%)
   - Читаемость
   - Комментарии
   - Обработка ошибок
   - Тесты

4. **Документация** (15%)
   - README понятный и полный
   - Примеры работают
   - Swagger docs доступны

5. **Инфраструктура** (10%)
   - Dockerfile оптимизирован
   - docker-compose настроен
   - Метрики корректные

---

## 💡 Советы для реализации

### 1. Начните с простого
- Сначала заставьте работать базовый функционал
- Потом оптимизируйте и улучшайте
- Не пытайтесь сделать всё идеально с первого раза

### 2. Коммитьте часто
```bash
git commit -m "feat: add video upload endpoint"
git commit -m "feat: implement motion detection"
git commit -m "feat: add prometheus metrics"
```

### 3. Тестируйте по ходу
- Пишите тесты сразу после реализации функции
- Не оставляйте тестирование на конец

### 4. Документируйте
- README пишите параллельно с кодом
- Добавляйте комментарии к сложным местам

### 5. Используйте готовые примеры
- FastAPI documentation
- OpenCV tutorials
- SQLAlchemy examples

---

## 📚 Полезные ресурсы

### Документация
- [FastAPI](https://fastapi.tiangolo.com/)
- [OpenCV Python](https://docs.opencv.org/4.x/d6/d00/tutorial_py_root.html)
- [SQLAlchemy](https://docs.sqlalchemy.org/)
- [Prometheus Client](https://github.com/prometheus/client_python)

### Туториалы
- [Motion Detection with OpenCV](https://www.pyimagesearch.com/2015/05/25/basic-motion-detection-and-tracking-with-python-and-opencv/)
- [FastAPI Best Practices](https://github.com/zhanymkanov/fastapi-best-practices)
- [Docker Best Practices](https://docs.docker.com/develop/dev-best-practices/)

### Инструменты
- **Postman** - тестирование API
- **DBeaver** - работа с PostgreSQL
- **Prometheus** - просмотр метрик
- **Docker Desktop** - управление контейнерами

---

## 🎉 Заключение

**VisionGuard** - отличное тестовое задание, которое проверяет:
- Навыки backend разработки
- Знание инфраструктуры
- Умение работать с видео
- Инженерное мышление

**Ключевые моменты для успеха:**
1. Чистая архитектура
2. Работающий код
3. Хорошие тесты
4. Понятная документация
5. Продуманная инфраструктура

**Время на реализацию:** 6-7 дней активной работы

---

**Версия**: 1.0  
**Дата анализа**: 12 ноября 2025  
**Статус**: ✅ Готов к реализации
